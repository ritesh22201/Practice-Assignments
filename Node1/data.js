const sum = (num1, num2) => {
    return num1 + num2;
}

const diff = (num1, num2) => {
    return num1 - num2;
}

const mul = (num1, num2) => {
    return num1 * num2;
}

const div = (num1, num2) => {
    return num1/num2;
}

module.exports = {sum, diff, mul, div};